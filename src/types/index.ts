export type ShareStatus = 'idle' | 'active' | 'expired' | 'error';

export type ThemeClasses = {
  pageBg: string;
  paperBg: string;
  line: string;
  marginLine: string;
  text: string;
  brand: string;
  panelBg: string;
  panelText: string;
  accent: string;
  accentBg: string;
  accentInput: string;
  badgeStrong: string;
  badgeBasic: string;
  statusIdle: string;
  statusActive: string;
  statusExpired: string;
  statusError: string;
  stamp: string;
};
